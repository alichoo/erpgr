{% extends 'base.html.twig' %}

{% block title %}Formgrh{% endblock %}

{% block body %}
    <h1>Formgrh</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formgrh.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ formgrh.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ formgrh.type }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ formgrh.date ? formgrh.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formgrh_index') }}">back to list</a>

    <a href="{{ path('app_formgrh_edit', {'id': formgrh.id}) }}">edit</a>

    {{ include('formgrh/_delete_form.html.twig') }}
{% endblock %}
