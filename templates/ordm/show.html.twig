{% extends 'base.html.twig' %}

{% block title %}Ordm{% endblock %}

{% block body %}
    <h1>Ordm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordm.id }}</td>
            </tr>
            <tr>
                <th>Nomper</th>
                <td>{{ ordm.nomper }}</td>
            </tr>
            <tr>
                <th>Fonction</th>
                <td>{{ ordm.fonction }}</td>
            </tr>
            <tr>
                <th>Aut</th>
                <td>{{ ordm.aut }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ ordm.lieu }}</td>
            </tr>
            <tr>
                <th>Mission</th>
                <td>{{ ordm.mission }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ordm_index') }}">back to list</a>

    <a href="{{ path('app_ordm_edit', {'id': ordm.id}) }}">edit</a>

    {{ include('ordm/_delete_form.html.twig') }}
{% endblock %}
