<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        <link href="{{asset('css/form1.css')}}" rel="stylesheet">
         <link href="{{asset('css/for1.css')}}" rel="stylesheet">
    </head>
    <body>
     <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand"href="{{ path('app_home') }}">
                <img src="{{ asset('imgs/imm.jpg')}}" alt="Agence Maritime" class="logo">
                K.DAMAK SHIPPING 
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item active">
                        <a class="nav-link"  href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('app_services') }}">Services</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('app_apropos') }}"">À Propos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('app_contact') }}">Contact</a>
                    </li>
                     {% if not app.user %}
                    <li class="nav-item">
                        <a class="nav-link btn btn-primary text-white"  href="{{ path('app_login') }}" data-toggle="modal">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link btn btn-secondary text-white"  href="{{ path('app_register') }}" data-toggle="modal">S'inscrire</a>
                       
                    </li>
                    {% else %}
                     <li class="nav-item">
                        <a class="nav-link btn btn-secondary text-white"  href="{{ path('app_logout') }}" data-toggle="modal">Se deconnecter</a>
                       
                    </li>
                     {% endif %}
                </ul>
            </div>
        </nav>
    </header>
    <main>
        <section id="home" class="hero">
            <div class="overlay"></div>
            <div class="container text-center">
                <h2>Bienvenue à notre Agence Maritime</h2>
                <p>Nous fournissons des services de transport maritime de qualité.</p>
                <a href="{{ path('app_home') }}" class="btn btn-warning">Nos Services</a>
            </div>
        </section>
        <!-- Autres sections de la page -->
    </main>
        {% block body %}{% endblock %}

         <footer class="bg-dark text-white text-center py-3">
        <div class="container">
            <p>&copy; 2024 Agence Maritime. Tous droits réservés.</p>
        </div>
    </footer>

    </body>
</html>
