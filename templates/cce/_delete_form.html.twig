<form method="post" action="{{ path('app_cce_delete', {'id': cce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cce.id) }}">
    <button class="btn">Delete</button>
</form>
